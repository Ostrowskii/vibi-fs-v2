name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout app
        uses: actions/checkout@v4

      - name: Rebuild skill artifacts
        run: |
          bash scripts/build_skill_artifacts.sh

      - name: Prepare site artifact
        run: |
          test -f index.html
          test -f play/index.html
          test -f fight/index.html
          test -f city-duel/index.html
          test -f lobby-test/index.html
          test -f game-test/index.html
          test -f story/index.html
          mkdir -p dist
          cp index.html dist/index.html
          mkdir -p dist/play
          cp play/index.html dist/play/index.html
          mkdir -p dist/fight
          cp fight/index.html dist/fight/index.html
          mkdir -p dist/city-duel
          cp city-duel/index.html dist/city-duel/index.html
          mkdir -p dist/lobby-test
          cp lobby-test/index.html dist/lobby-test/index.html
          mkdir -p dist/game-test
          cp game-test/index.html dist/game-test/index.html
          mkdir -p dist/story
          cp story/index.html dist/story/index.html
          mkdir -p dist/assets/skills
          cp assets/skills/*.svg dist/assets/skills/
          touch dist/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
